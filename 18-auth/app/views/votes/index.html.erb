<ul>
<% @votes.each do |vote| %>
  <li>
    <%= vote.user.username %> voted for #<%= vote.color.hex %>
    <% if vote.user_id == @logged_in_user.id %>
    <%= button_to "delete vote", vote_path(vote), method: :delete %>
    <% end %>
  </li>
<% end %>
</ul>