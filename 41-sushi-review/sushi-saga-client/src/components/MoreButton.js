import React from 'react'

const MoreButton = (props) => {

  // event listner handleMoreButtonClick()
  return <button onClick={props.handleMoreButtonClick}>
    More sushi!
          </button>
}

export default MoreButton