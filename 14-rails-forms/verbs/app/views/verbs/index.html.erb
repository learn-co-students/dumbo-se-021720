<h1>All Verbs</h1>

<%= link_to "New verb", new_verb_path %>

<ul>
<% @verbs.each do |verb| %>
  <li>
  <!--   <a href="<%= verb_path(verb) %>">
      <%= verb.name %>  
    </a> -->
    <%= link_to verb.name, verb, class: "potato" %>
  </li>
<% end %>
</ul>